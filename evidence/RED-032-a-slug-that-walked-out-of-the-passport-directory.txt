# RED-032 - a passport slug that walked out of the passport directory
#
# Produced by evidence/RED-032-generator.py, checked in beside this file so the runs below can be
# repeated rather than believed. It establishes that LAW-SLUG-JUDGED-BEFORE-FETCH CAN FAIL
# (invariant 5) in each of the directions it holds.
#
# THE FINDING WAS NOT A SCANNER'S. CodeQL raised #6 and #7 against `web-1.0/src/App.tsx`, the
# FROZEN phase-2 rollback point, and both were dismissed on 2026-08-24 because the deploy builds
# `web/` only and nothing under `web-1.0/` is ever served. That dismissal is correct and it is not
# the whole reading: the frozen copy built its fetch through `.replace(/[:/]/g, "_")`, which
# removes the separator, and the LIVE `web/src/App.tsx` interpolated the route substring raw.
# The live tree was a superset of the code the scanner objected to in the dead one, and carried no
# alert of its own. Absence of an alert on the product path is `not_measured`, not `clean`.
#
# THE SIX MUTATIONS ARE NOT SIX SPELLINGS OF ONE. Four attack the rule (the class, the anchors, the
# empty case, the type coercion) and two attack its wiring - the guard nobody calls, and the
# refusal rendered as a measured absence. The last two are the ones a diff would not catch: in both
# the module is correct, present, imported and green on its own terms.
#
# Each mutation is applied to a pristine copy, its marker grepped back out to prove the edit
# landed, run with its output captured in its OWN buffer, and the file restored and compared by
# sha256 before the next one - because RED-013 came to carry one mutation's output under another's
# heading when four runs shared one redirect (L-26). The generator refuses to write this file if
# any mutation exits other than 1 (exit 2 is a file that stopped parsing, which is a red about
# nothing), if any two mutations kill the same set of tests, or if any mutation kills
# `test_the_probe_can_fail` - the suite's instrument control, a POSITIVE reading that an inert
# probe cannot produce, and the reason the refusals here are worth something.
#
# The gate is web/src/slug.js and the suite RUNS it under Node
# (tests/slug_probe.mjs) rather than matching patterns against its source. That is the point of it:
# `^[A-Za-z0-9_-]+$` accepts a trailing newline in Python's `re` and refuses it in JavaScript, so a
# gate that re-implemented the rule in the test's own language would have asserted a property the
# browser does not have.
#
# Everything below the line in each block is verbatim tool output.

====================================================================================================
MUTATION 1 - the character class admits the separator - the defect exactly as it stood
====================================================================================================

Applied to web/src/slug.js:

  - const SLUG = /^[A-Za-z0-9_-]+$/;
  + const SLUG = /^[A-Za-z0-9_/-]+$/; // MUTATION-1

Marker 'MUTATION-1' grepped back out of the file after the edit: yes.
Tests killed (1): test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a/b']

--- verbatim output of `python -m pytest tests/test_passport_slug_is_judged_before_it_is_fetched.py -q -rf` ---

.........F...............                                                [100%]
=================================== FAILURES ===================================
_____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a/b'] ______

slug = 'a/b'
why = 'an inner slash - the exact residue `replace(/\\/$/, "")` does not remove'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'a/b' - an inner slash - the exact residue `replace(/\/$/, "")` does not remove
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
=========================== short test summary info ============================
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a/b']
1 failed, 24 passed in 1.01s

====================================================================================================
MUTATION 2 - the anchors are dropped, so the class need only appear SOMEWHERE in the slug
====================================================================================================

Applied to web/src/slug.js:

  - const SLUG = /^[A-Za-z0-9_-]+$/;
  + const SLUG = /[A-Za-z0-9_-]+/; // MUTATION-2

Marker 'MUTATION-2' grepped back out of the file after the edit: yes.
Tests killed (11): test_the_adversarial_slug_is_refused_and_never_reaches_a_url['..%2f..%2fetc'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a#frag'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a%2Fb'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a/b'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a?cache=1'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a\\nb'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['abc\\n'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['git x'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['git_x/../../etc/passwd'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['https://evil.example.com/x'], test_the_adversarial_slug_is_refused_and_never_reaches_a_url['registry.json']

--- verbatim output of `python -m pytest tests/test_passport_slug_is_judged_before_it_is_fetched.py -q -rf` ---

........FFFF.FFFFFFF.....                                                [100%]
=================================== FAILURES ===================================
_ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['git_x/../../etc/passwd'] _

slug = 'git_x/../../etc/passwd'
why = 'traversal out of the passport directory, the plain form'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'git_x/../../etc/passwd' - traversal out of the passport directory, the plain form
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
_____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a/b'] ______

slug = 'a/b'
why = 'an inner slash - the exact residue `replace(/\\/$/, "")` does not remove'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'a/b' - an inner slash - the exact residue `replace(/\/$/, "")` does not remove
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a%2Fb'] _____

slug = 'a%2Fb'
why = 'a percent-encoded slash, which some origins resolve before the path is matched'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'a%2Fb' - a percent-encoded slash, which some origins resolve before the path is matched
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
_ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['..%2f..%2fetc'] _

slug = '..%2f..%2fetc'
why = 'traversal with the separator encoded, in case only a literal `/` is checked'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS '..%2f..%2fetc' - traversal with the separator encoded, in case only a literal `/` is checked
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['abc\\n'] ____

slug = 'abc\n'
why = "a trailing newline - accepted by the same pattern in Python's `re`, refused in JS"

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'abc\n' - a trailing newline - accepted by the same pattern in Python's `re`, refused in JS
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a\\nb'] _____

slug = 'a\nb'
why = 'an embedded newline, in case the anchors were relaxed to multiline'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'a\nb' - an embedded newline, in case the anchors were relaxed to multiline
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
_ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['registry.json'] _

slug = 'registry.json'
why = 'a dot, which is what lets a slug name a file other than a passport'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'registry.json' - a dot, which is what lets a slug name a file other than a passport
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
__ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a?cache=1'] ___

slug = 'a?cache=1'
why = 'a query string, which changes the request without changing the directory'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'a?cache=1' - a query string, which changes the request without changing the directory
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a#frag'] ____

slug = 'a#frag', why = 'a fragment'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'a#frag' - a fragment
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
_ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['https://evil.example.com/x'] _

slug = 'https://evil.example.com/x'
why = 'an absolute URL pasted into a path segment'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'https://evil.example.com/x' - an absolute URL pasted into a path segment
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
____ test_the_adversarial_slug_is_refused_and_never_reaches_a_url['git x'] _____

slug = 'git x', why = 'a space'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS 'git x' - a space
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
=========================== short test summary info ============================
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['git_x/../../etc/passwd']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a/b']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a%2Fb']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['..%2f..%2fetc']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['abc\\n']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a\\nb']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['registry.json']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a?cache=1']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['a#frag']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['https://evil.example.com/x']
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['git x']
11 failed, 14 passed in 1.01s

====================================================================================================
MUTATION 3 - the empty slug is admitted, so `/p//` requests the passport directory itself
====================================================================================================

Applied to web/src/slug.js:

  - const SLUG = /^[A-Za-z0-9_-]+$/;
  + const SLUG = /^[A-Za-z0-9_-]*$/; // MUTATION-3

Marker 'MUTATION-3' grepped back out of the file after the edit: yes.
Tests killed (1): test_the_adversarial_slug_is_refused_and_never_reaches_a_url['']

--- verbatim output of `python -m pytest tests/test_passport_slug_is_judged_before_it_is_fetched.py -q -rf` ---

............F............                                                [100%]
=================================== FAILURES ===================================
_______ test_the_adversarial_slug_is_refused_and_never_reaches_a_url[''] _______

slug = ''
why = 'the empty slug: `/p//` would otherwise request the directory itself'

    @pytest.mark.parametrize(("slug", "why"), REFUSED, ids=[repr(s) for s, _ in REFUSED])
    def test_the_adversarial_slug_is_refused_and_never_reaches_a_url(slug: str, why: str) -> None:
        """The boolean is not the whole property; where the request WOULD have gone is the property.
    
        Both halves are asserted in the same node because they are one claim about one slug, and
        splitting them produced two tests that could only ever fail together - which is a failure set
        that cannot tell two mutations apart.
        """
        row = probe([slug])[0]
        assert row["slug"] == slug, "the probe answered about a different slug than it was asked"
>       assert not row["safe"], f"the guard ACCEPTS {slug!r} - {why}"
E       AssertionError: the guard ACCEPTS '' - the empty slug: `/p//` would otherwise request the directory itself
E       assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:144: AssertionError
=========================== short test summary info ============================
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_adversarial_slug_is_refused_and_never_reaches_a_url['']
1 failed, 24 passed in 0.90s

====================================================================================================
MUTATION 4 - the type check goes, and `RegExp.test` coerces a non-string into a matching one
====================================================================================================

Applied to web/src/slug.js:

  - return typeof slug === "string" && SLUG.test(slug);
  + return SLUG.test(slug); // MUTATION-4

Marker 'MUTATION-4' grepped back out of the file after the edit: yes.
Tests killed (1): test_a_non_string_is_refused

--- verbatim output of `python -m pytest tests/test_passport_slug_is_judged_before_it_is_fetched.py -q -rf` ---

.....................F...                                                [100%]
=================================== FAILURES ===================================
_________________________ test_a_non_string_is_refused _________________________

    def test_a_non_string_is_refused() -> None:
        """`RegExp.test` coerces, so a missing `typeof` check is a hole with no visible edit.
    
        `SLUG.test(null)` tests the STRING `"null"`, which is inside the character class - so a guard
        without the type check answers `true` for a value that is not a slug at all, and the caller
        goes on to request `/data/passports/null.json`. Kept separate from the corpus above because
        `null` has no Python spelling that round-trips through the URL assertion, and forcing it into
        that list would have meant weakening the assertion for every other case.
        """
        rows = probe([None, 1, [], {}])
        for row in rows:
>           assert not row["safe"], (
                f"the guard accepts {row['slug']!r}, which is not a string. `RegExp.test` would have "
                f"coerced it and matched the coercion rather than the value."
            )
E           AssertionError: the guard accepts None, which is not a string. `RegExp.test` would have coerced it and matched the coercion rather than the value.
E           assert not True

tests/test_passport_slug_is_judged_before_it_is_fetched.py:162: AssertionError
=========================== short test summary info ============================
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_a_non_string_is_refused
1 failed, 24 passed in 0.84s

====================================================================================================
MUTATION 5 - the guard is correct and NOBODY CALLS IT - L-21's shape, the repair that documents itself into looking done
====================================================================================================

Applied to web/src/App.tsx:

  -     if (!isSafeSlug(slugInRoute)) {
  -       setPassports((p) => ({ ...p, [key]: { state: "invalid" } }));
  -       return;
  -     }
  +     // MUTATION-5: the call removed, the module left in place and still imported.

Marker 'MUTATION-5' grepped back out of the file after the edit: yes.
Tests killed (1): test_the_guard_is_called_before_the_fetch

--- verbatim output of `python -m pytest tests/test_passport_slug_is_judged_before_it_is_fetched.py -q -rf` ---

.......................F.                                                [100%]
=================================== FAILURES ===================================
__________________ test_the_guard_is_called_before_the_fetch ___________________

    def test_the_guard_is_called_before_the_fetch() -> None:
        """The wiring half. A correct module nobody calls is an unarmed gate that reads as an armed one.
    
        Position, not presence: L-21 is the case where three repairs were written, documented, and
        never called, and `grep` found each of them at its own definition and nowhere else.
        """
        src = APP.read_text()
        assert 'from "./slug"' in src, "App.tsx does not import the guard at all"
    
        # The effect that fetches a passport, from the guard clause that opens it to the `fetch(`.
        effect = re.search(r"if \(!slugInRoute\) return;(.*?)fetch\(`/data/passports/", src, re.S)
        assert effect, (
            "could not find the passport effect in App.tsx. Either it was restructured - in which "
            "case this assertion must be rewritten rather than deleted - or the fetch no longer "
            "interpolates the slug, which would make the guard unnecessary and this test wrong."
        )
>       assert "isSafeSlug(slugInRoute)" in effect.group(1), (
            "the passport fetch is reached without `isSafeSlug(slugInRoute)` between the effect's "
            "entry and the request. The guard exists and does not stand in front of anything."
        )
E       AssertionError: the passport fetch is reached without `isSafeSlug(slugInRoute)` between the effect's entry and the request. The guard exists and does not stand in front of anything.
E       assert 'isSafeSlug(slugInRoute)' in '\n    const key = passportId ?? slugInRoute;\n    if (passports[key]) return;\n    // THE SLUG IS JUDGED BEFORE IT RE... the module left in place and still imported.\n    setPassports((p) => ({ ...p, [key]: { state: "loading" } }));\n    '
E        +  where '\n    const key = passportId ?? slugInRoute;\n    if (passports[key]) return;\n    // THE SLUG IS JUDGED BEFORE IT RE... the module left in place and still imported.\n    setPassports((p) => ({ ...p, [key]: { state: "loading" } }));\n    ' = <built-in method group of re.Match object at 0x7fb5dcccb130>(1)
E        +    where <built-in method group of re.Match object at 0x7fb5dcccb130> = <re.Match object; span=(12726, 13546), match='if (!slugInRoute) return;\n    const key = passpo>.group

tests/test_passport_slug_is_judged_before_it_is_fetched.py:200: AssertionError
=========================== short test summary info ============================
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_the_guard_is_called_before_the_fetch
1 failed, 24 passed in 0.82s

====================================================================================================
MUTATION 6 - the refusal renders as a missing passport - invariant 1 at the point a reader sees it
====================================================================================================

Applied to web/src/App.tsx:

  -     if (p.state === "invalid")
  +     if (p.state === "__never__") // MUTATION-6

Marker 'MUTATION-6' grepped back out of the file after the edit: yes.
Tests killed (1): test_an_invalid_slug_is_not_reported_as_a_missing_passport

--- verbatim output of `python -m pytest tests/test_passport_slug_is_judged_before_it_is_fetched.py -q -rf` ---

........................F                                                [100%]
=================================== FAILURES ===================================
__________ test_an_invalid_slug_is_not_reported_as_a_missing_passport __________

    def test_an_invalid_slug_is_not_reported_as_a_missing_passport() -> None:
        """Invariant 1, at the point a reader sees it.
    
        `missing` is what the registry answered when we asked. A slug we refused was never asked
        about, and rendering "nothing has been issued under this identifier" over it would publish an
        absence nobody measured - the founding defect with the sign flipped. The two must be different
        states, and the fifth state must reach the renderer.
        """
        src = APP.read_text()
        assert '{ state: "invalid" }' in src, "the refusal does not have a state of its own"
>       assert 'p.state === "invalid"' in src, (
            "nothing renders the `invalid` state, so a refused slug falls through to the skeleton and "
            "the reader waits for ever on a request that was never made"
        )
E       AssertionError: nothing renders the `invalid` state, so a refused slug falls through to the skeleton and the reader waits for ever on a request that was never made
E       assert 'p.state === "invalid"' in '/** Path routing, hand-rolled, and rendered to static HTML at build time.\n *\n * WHY NOT HASH, ANY MORE. Routing was...route={route} containerRef={top}>\n      <Body route={route} reg={reg} passport={passport} />\n    </Shell>\n  );\n}\n'

tests/test_passport_slug_is_judged_before_it_is_fetched.py:216: AssertionError
=========================== short test summary info ============================
FAILED tests/test_passport_slug_is_judged_before_it_is_fetched.py::test_an_invalid_slug_is_not_reported_as_a_missing_passport
1 failed, 24 passed in 0.88s

====================================================================================================
THE SUITE, GREEN, BEFORE ANY MUTATION AND AFTER THE LAST RESTORE
====================================================================================================

before:

.........................                                                [100%]
25 passed in 0.90s

after (both subjects compared by sha256 against their pristine bytes first):

.........................                                                [100%]
25 passed in 0.87s
