/** Types for `slug.js`. The implementation is plain JavaScript so the gate can execute the same
 *  bytes the bundle ships; see the header of `slug.js` for why. */
export declare function isSafeSlug(slug: unknown): slug is string;
