RED-025 - A NOTE THE REPOSITORY'S OWN GATES REFUSE HAD NOTHING BETWEEN IT AND THE LIVE SITE

Generated by `evidence/RED-025-generator.py`. Everything below a heading is verbatim output of the
run named in that heading; nothing is retyped. The subjects are `~/orchestra/notes_cron.py` and
`~/orchestra/notes_gen.py`, which are outside this repository by D-17 and D-19, and the fixture is
`~/orchestra/quarantine/20260824T063401Z/` - the two notes the scheduled cycle really captured on
2026-08-24, one of which fails `tests/test_notes.py` and one of which does not.

WHAT A READER SHOULD CHECK RATHER THAN TRUST. Every claim here is a mutation or a control, because a
red run proves a gate is armed against the mutation that produced it and against no other (L-26):

  * A1 shows two instruments disagreeing about one tree. Re-runnable by hand in ten seconds.
  * A2 and A3 are the two branches of the SAME red gate. They differ in what happens to the
    capture, and the difference is a second measurement rather than a rule about whose file it is.
  * A3b, A3d and A3e are one defect found three times, each inside the fix for the last: the exit
    code could not see two causes, then the test COUNT could not see two causes inside one test
    function, then the id SET could not see a cause the door's `| tail -3` had truncated away. Read
    them in that order; the third needs no race and no changing tree, and A3 remains the control
    that a capture which really is innocent still goes back into the tree.
  * A5 removes the gate call and the red note reaches the deploy door. A4 is worth nothing without
    it: a cycle that would have stopped anyway proves no gate.
  * B2's control is the green note reaching the tripwire. Without it, "refused before a model call"
    would be a claim about a code path nobody watched execute.
  * B3 is not a mutation at all - it is the generator's own previous revision, fetched from
    `~/orchestra`'s history, so the "before" state is the one that really existed.
  * B4 and B5 kill the two copies of the placement rule separately, because a rule read in two
    places can be armed in one and decorative in the other (L-21).

WHAT IS NOT ASSERTED HERE, AND THE LIST IS LONGER THAN THE FIRST DRAFT'S.

  * NOT that a red note can never reach the deploy. What is measured is that the note in the tree
    WHEN THE GATES RAN cannot. `step_gates` reads the tree once, and `deploy.sh` ships the tree as
    it stands several steps later, so a note written into `web/notes/src/` by a concurrent capture
    in between would pass `step_tree` by construction - both its halves are cycle-owned - and go
    out. Narrow, and it needs a second writer, but "cannot reach" is a universal and this is not
    one. Named in `~/orchestra/FINDINGS.md`. Found by Fable.
  * NOT that the fixture note is the only note that could fail these gates. The placement rule is
    one of several the suite holds, and `step_gates` is indifferent to which one goes red.
  * NOT that the deploy would have succeeded in A5. `DEPLOY` is a stub; what A5 measures is that
    the cycle REACHED it.
  * NOTHING at all about whether provek.dev currently serves a bad note. That is a reading of the
    origin, and this file reads a working tree.

RE-RUNNING IT NEEDS A COMMITTED TREE. A1's control is `scripts/publishable_tree.py`, which counts
any uncommitted file - including this generator and this artefact - as foreign work and refuses.
The instrument therefore declines to demonstrate a clean-tree property on a dirty tree, which is
correct of it and is why the generator is committed before the artefact it writes is regenerated.

HOW THIS FILE BEHAVED WHILE IT WAS BEING WRITTEN, WHICH BELONGS HERE RATHER THAN IN A COVER NOTE.
Re-running it should reproduce this artefact except for timestamps and pytest durations; two
consecutive runs of THIS revision did, compared with `git diff` against the committed copy. The
three-run comparison quoted here before was of the revision without A3e and is not evidence about
this one. A run of a still earlier revision exited without writing the artefact, and the cause was
lost because only the last four lines of its output were kept. It has not recurred and it is not
explained. What was fixed is the
reason it could not be diagnosed: the block printer now flushes, and a `Bail` prints on both
streams, because stdout is block-buffered when piped and stderr is not, so the refusal had been
arriving above the block it followed. An unexplained failure in the instrument is worth less than
an explained one and more than a silence, so it is written here rather than left out.

==================================================================================================
RUN A1 - THE DEFECT: the gates refuse the note and the cycle's only guard waves it through
==================================================================================================

The tree now holds `web/notes/src/autonomy-levels-l0-l5.md` and its manifest line, exactly as the
05:22 capture left them on 2026-08-24. Two instruments read the same tree:

$ ./scripts/push.sh --gates-only
exit 1
All checks passed!
FAILED tests/test_notes.py::test_the_primary_key_is_where_it_was_promised_and_not_repeated_into_the_ground
1 failed, 592 passed, 1 skipped in 20.57s

$ python3 scripts/publishable_tree.py --root .
exit 0
PUBLISHABLE: nothing dirty outside the 4 cycle-owned paths (2 cycle-owned path(s) dirty).

The repository says the tree is red. The one check the publication cycle ran before
`deploy.sh` says it is publishable, and it is not wrong - `web/notes/src/` is the
cycle's own output and the question it asks is about foreign work. Between those two
readings there was nothing at all.

==================================================================================================
RUN A2 - THE FIX: step_gates parks the capture and ends the cycle RED
==================================================================================================

`step_gates(['autonomy-levels-l0-l5'])` against the same tree, with the real `scripts/push.sh` and
the real repository:

  [parked_for_attribution  ] gates    the gates are red and this cycle captured; the capture is out of the tree so that a second reading can say whether it was the cause
  [parked_red_capture      ] gates    All checks passed! | FAILED tests/test_notes.py::test_the_primary_key_is_where_it_was_promised_and_not_repeated_into_the_ground | 1 failed, 592 passed, 1 skipped in 19.87s

raised: Red: the capture ['autonomy-levels-l0-l5'] fails the repository gates
note removed from web/notes/src/: True
parked copies: ['autonomy-levels-l0-l5.20260824T101154Z.md']
parked sha256 == the note's sha256: True
manifest back to its pre-fixture bytes: True
reason file beside it (first lines; the whole gate output follows them in the file):
parked_at: 2026-08-24T10:11:54Z
slug: autonomy-levels-l0-l5
sha256: dd4a0da92c9db6d027c8e92dadc2fcf11a8291e8b96fb3db000b40cd70cbcf64
manifest_line: {"body_sha256": "736ce8b5f7adb7dd1a3fd1adf778ce7058dbfd96bb6873ad8a8ab582985f2524", "date_published": "2026-08-24", "date_modified": "2026-08-24"}

WHY THIS NOTE IS NOT IN THE TREE

the repository gates refused the tree this note was captured into.

The second gate reading is what convicts the note: the same tree passes without it.
Nothing was deleted - the capture is fourteen minutes of model calls and the only
record of what a model wrote when the rule was broken.

==================================================================================================
RUN A3 - ATTRIBUTION: a red that is not the capture's does not cost the capture
==================================================================================================

The tree now holds a note that PASSES the placement gate and a test file that fails on
purpose. A gate step that assumed the capture was to blame would park a good note over
somebody else's red test, and write a journal line that reads as an attribution
nothing measured.

  [parked_for_attribution  ] gates    the gates are red and this cycle captured; the capture is out of the tree so that a second reading can say whether it was the cause
  [RED                     ] gates    the same 1 test(s) fail with the capture removed and none of them judges the note corpus, so the capture adds no failure and is back in the tree uncha

raised: Red: the repository gates refuse the tree for a reason older than this cycle
the note is back in the tree byte for byte: True
parked copies left behind: []

Both readings are red and both name the same failing test - the fixture's, never the
note's - and that is the whole of the attribution:
the capture is exonerated by measurement rather than by a rule about which files are
whose. The cycle is RED either way; what the second reading decides is what happens to
the note.

==================================================================================================
RUN A3b - a red with TWO causes does not acquit the capture
==================================================================================================

The tree holds the note that fails the placement gate AND the unrelated failing test.
Both readings are red, so a step that compared exit codes would restore the note - and
leave it in the manifest, where it is never recaptured and every later cycle wedges on
`captured=[]` under a journal line denying it had anything to do with the red. The
morning that produced this task was one failing test away from that state.

  [parked_for_attribution  ] gates    the gates are red and this cycle captured; the capture is out of the tree so that a second reading can say whether it was the cause
  [parked_red_capture      ] gates    the tree is red for another reason as well - 1 test(s) fail without this cycle's capture and 2 with it - which acquits nobody

raised: Red: the capture ['autonomy-levels-l0-l5'] adds 1 failure(s) to a tree that was already red
note still out of the tree: True
parked by this run: ['autonomy-levels-l0-l5.20260824T101336Z.md']

Two failures with the capture, one without: the difference is what the capture added,
and the tree being red for another reason as well acquits nobody. The comparison rides
the COUNT rather than the printed ids, because `scripts/push.sh` ends its test step with
`| tail -3` and a third failure would push an id off the end - two readings could then
print the same two names while differing by one test, and a set comparison would call
that an exoneration.

==================================================================================================
RUN A3d - two offenders, one failing test: an equal count is not an acquittal
==================================================================================================

The tree holds this cycle's capture AND a second note breaking the same rule. The
placement gate is one unparametrised function looping over the corpus, so it fails ONCE
either way: both readings print `1 failed` and name the same id.

  [parked_for_attribution  ] gates    the gates are red and this cycle captured; the capture is out of the tree so that a second reading can say whether it was the cause
  [parked_attribution_not_measured] gates    the same tests fail either way, and ['tests/test_notes.py::test_the_primary_key_is_where_it_was_promised_and_not_repeated_into_the_ground'] judge the 

raised: Red: the gates refuse the tree and the capture's part in it could not be measured
note out of the tree: True

A comparison of counts alone calls this 'the same tests fail without the capture' and
restores a guilty note - which then sits in the manifest, is never recaptured, and
wedges every later cycle on `captured=[]` under yesterday's journal line saying the red
was not this cycle's. It is the exit-code defect one level down: the code was blind to
two causes, then the count was blind to two causes inside one test. What separates this
from A3 is not the count but WHICH test fails - a module under `tests/test_notes*` judges
the corpus, and an equal count there establishes nothing about this capture.
Found by Fable, in the fix for the first blindness.

==================================================================================================
RUN A3e - the name the acquittal turns on is the one the door truncated away
==================================================================================================

A3d's tree plus TWO unrelated failing tests. Three tests fail in both readings, and
`scripts/push.sh` ends its test step with `| tail -3`, so only the last two `FAILED`
lines survive. The corpus test sorts first and is the one that falls off.

  [parked_for_attribution  ] gates    the gates are red and this cycle captured; the capture is out of the tree so that a second reading can say whether it was the cause
  [parked_attribution_not_measured] gates    the door printed 2 and 2 failing test names for readings that declare 3 - `| tail -3` in scripts/push.sh dropped the rest, so whether a corpus-level t

raised: Red: the gates refuse the tree and the capture's part in it could not be measured
note out of the tree: True

A3d's fix reads the printed ids to find a corpus-level test among them. Here the ids do
not contain one - not because none failed, but because the door printed two names for a
reading that declares three. An empty classification then reads as 'no corpus test
failed', which is the acquittal A3d exists to prevent, reached by a different road. It
is the same blindness a third level down: the exit code was blind to two causes, the
count to two causes in one test, and the id set to a cause outside the tail. Neither a
race nor a changing tree is needed - both readings here are byte-identical.

What closes it is that the COUNT survives what truncates the names, so the count audits
them: fewer names than the count declares means the set is incomplete, and a
classification drawn from an incomplete set is unknowable rather than negative. Only
the equal-count path is guarded - where the capture strictly adds failures the count
convicts on its own and no name is load-bearing (A3b is that control, and still passes).

==================================================================================================
RUN A3c - a red the comparison cannot read is a third state, not a verdict
==================================================================================================

The tree holds a note that PASSES the gates and a module in `scripts/` bound to no
`ABI-*` requirement, so the door refuses at step 2 of 7 and the suite never runs. There
is no failing test to count in either reading, so the counterfactual asks a question the
instrument cannot answer.

  [parked_for_attribution  ] gates    the gates are red and this cycle captured; the capture is out of the tree so that a second reading can say whether it was the cause
  [parked_attribution_not_measured] gates    at least one reading named no failing test - the door refused before the suite, or the suite failed on something that is not a test - so there is no count to compare; whether the capture contributed is NOT MEASURED, and it is parked rather than restored because parking is reversible from a verified copy and a wedged cycle is not. A person should read this line.

raised: Red: the gates refuse the tree and the capture's part in it could not be measured
note out of the tree: True

The capture stays parked and the journal says why in the state's own name. That is a
CHOICE and it is recorded as one: parking is reversible from a copy this step verified
by sha256 before removing anything, and the alternative - restoring on an unmeasured
acquittal - is the wedge A3b describes, arrived at silently. Note that this branch
parks an innocent note, which is the price, and it is the reversible half of the trade.

==================================================================================================
RUN A4 / MUTATION A5 - the whole cycle, with the step and without it
==================================================================================================

Both runs are `run_cycle()` over the SAME tree, the one holding the red note. `DEPLOY`
is a stub that writes a marker file, so 'the deploy was reached' is a fact on disk -
and so that the mutation stops at a marker instead of publishing the note.

A4, the file as it stands:
  [open                    ] cycle    
  [clear                   ] host     
  [nothing_pending         ] capture  every topic in notes_topics.json is already in the manifest
  [ok                      ] build        /p/git_whiteknightonhorse_provek/
  [RED                     ] gates    All checks passed! | FAILED tests/test_notes.py::test_the_primary_key_is_where_it_was_promised_and_not_repeated_into_the_ground | 1 failed, 592 passed, 1 skipped in 20.20s
  [RED                     ] cycle    the repository gates refuse the tree
  steps journalled : ['cycle', 'host', 'capture', 'build', 'gates', 'cycle']
  exit code        : 1   (1 = RED)
  deploy reached   : False

A5, the same file with one line removed:
--- /home/incubator/orchestra/notes_cron.py
-step_gates(cap["captured"])
+pass  # MUTATION: the gate step removed
  [open                    ] cycle    
  [clear                   ] host     
  [nothing_pending         ] capture  every topic in notes_topics.json is already in the manifest
  [ok                      ] build        /p/git_whiteknightonhorse_provek/
  [ok                      ] sitemap  
  [publishable             ] tree     PUBLISHABLE: nothing dirty outside the 4 cycle-owned paths (2 cycle-owned path(s) dirty).
  [ok                      ] deploy   both streams empty
  [RED                     ] published a URL this cycle published does not answer 200 on the live site
  [RED                     ] cycle    a newly published URL is not live
  steps journalled : ['cycle', 'host', 'capture', 'build', 'sitemap', 'tree', 'deploy', 'published', 'cycle']
  exit code        : 1
  deploy reached   : True

The mutation is the measurement of the gate. Without that one call the cycle carries a
note the repository's own tests refuse all the way to the door of the publication
channel.

WHY A5's EXIT CODE IS ALSO 1, AND WHY THAT IS NOT THE GATE DOING ITS JOB. The mutated
run goes red at `published`, three steps AFTER the deploy: `DEPLOY` is a stub that
publishes nothing, so the URL the build made new does not answer 200 when the cycle
reads it back. Against the real `deploy.sh` that URL would have been live, `published`
would have been green, and the cycle would have gone on to submit the address of a page
failing this repository's own tests to Bing. Both exit codes are 1 and they mean
opposite things - which is exactly why the marker file is what this run measures and
the exit code is not. This paragraph is reasoning about a run that was deliberately not
made, and it is marked as such rather than presented as a reading.

==================================================================================================
RUN B1 - the rule the tests held alone, measured at capture time
==================================================================================================

`measure_all()` over the two notes that scheduled cycle really captured, with the fold
imported from `tests/notes_support.py` - the gate's own reader, not a second copy:

  autonomy-levels-l0-l5
    title       : 'What the L0-L5 autonomy ladder actually measures - Provek'
    primary key : ('ai agent autonomy levels', 'declared')
    placement misses: 3
      - primary key 'ai agent autonomy levels' is not in the title: 'What the L0-L5 autonomy ladder actually measures   [fatal=True]
      - primary key 'ai agent autonomy levels' is not in the h1: 'The L0-L5 autonomy ladder'  [fatal=True]
      - primary key 'ai agent autonomy levels' is not in the description: "Describes how Provek's L0-L5 ladder assigns  [fatal=True]

  what-erc-8004-provides-and-what-it-does-not
    title       : 'ERC-8004: what it standardizes, what it leaves open - Provek'
    primary key : ('erc 8004', 'declared')
    placement misses: 0

One fails in three places and one passes, so the check is not a ritual refusal: it
discriminates between two bodies of prose a model wrote twenty minutes apart. The lead
of the failing note DOES carry the key, which is why the miss is `fatal` - a paragraph
rewriter cannot reach a title.

==================================================================================================
RUN B2 - the refusal lands after the PLAN and before the prose, and the control proves it
==================================================================================================

`build()` with `plan_note` answered from the note's own captured front matter, and
`ask`/`ask_json` replaced by a tripwire that raises if a model call is attempted:

  autonomy-levels-l0-l5
    -> Refusal: autonomy-levels-l0-l5: the plan places the primary key wrong on 3 count(s); refused before any prose was written

  what-erc-8004-provides-and-what-it-does-not
    -> AssertionError: TRIPWIRE: a model call was attempted

The control is the whole of the reading. A refusal on its own says nothing about WHEN
it happened; the green note reaching the tripwire says the capture goes on to write
prose when the placement is right, so the red note's refusal is attributable to the
placement and not to the harness.

ONE MODEL CALL IS STILL SPENT, AND AN EARLIER DRAFT OF THIS HEADING DENIED IT. The plan
is itself a call - `plan_note()` is `ask_json(claude-sonnet-5, ...)`, and the real run's
journal has it at 05:23:24 - and the placement rule cannot be read before it, because
the title it judges is what the plan produces. The refusal stands between the plan and
the prose. In this harness the plan is answered from the fixture, so the tripwire does
not see that call by construction: 'before the first model call' would have been a claim
about the harness's own shape. Found by Fable.

What the refusal saves is counted rather than described: the plan it stopped carries
6 sections, so the calls not made are 6 prose calls plus the lead, plus the FAQ
answers, plus up to REPAIR_MAX (6) repair calls - on a note that could not have been
published by any of them.

==================================================================================================
RUN B3 - the same note through the generator as it stood before this commit
==================================================================================================

Not a mutation: `git show c46a489:notes_gen.py` out of `~/orchestra`'s own history. The
revision is chosen by a PROPERTY rather than by hand - the newest one whose blob does
not define `measure_placement` - so re-running this file after later commits cannot
quietly move the measurement onto the wrong code. Same tripwire:

  autonomy-levels-l0-l5 -> AssertionError: TRIPWIRE: a model call was attempted

The model call is reached. Before this commit nothing in the capture read the placement
rule at all, so a plan that put the primary key nowhere near the title bought six prose
calls, a lead, the FAQ answers and a published note.

WHAT THIS RUN IS NOT, AND THE DRAFT THAT SAID IT WAS. The first version of this
paragraph called c46a489 'the file that captured this note at 05:22'. It is not, and the
note says so itself: its `provenance.generator_sha256` is b7656ef85b44343a..., which is the
blob of 03cf9b0 - a revision committed at 05:31:56, nine minutes INTO a run that had
started at 05:22 and whose journal logs `write`, a step that revision no longer has.
`build()` hashes the generator by re-reading it from disk at the END of a fourteen-minute
capture, so a file edited mid-run is pinned by a note the previous code produced. That is
a provenance field claiming more than its artefact, in the project built to find those;
it is recorded in `~/orchestra/FINDINGS.md` and not fixed here, because it is a change to
what a published note's provenance MEANS and needs its own red run. Neither revision
carries the placement rule, so this measurement stands under either.

==================================================================================================
MUTATIONS B4 / B5 - each copy of the rule killed on its own
==================================================================================================

The rule is read twice: once the moment the plan exists, and once over the assembled
body inside `measure_all()`. The first is an accelerator and the second is the one
nothing can be staged past, so each has to be shown to be armed by itself.

B4 - the early reading removed:
--- /home/incubator/orchestra/notes_gen.py
-if early:
+if False:  # MUTATION: the early refusal disarmed
  autonomy-levels-l0-l5 -> AssertionError: TRIPWIRE: a model call was attempted
  The tripwire fires: without that call the capture spends prose calls on a note that
  cannot be published. What this run does NOT show is the later refusal - the tripwire
  ends the capture at the first model call, so nothing here executes `measure_all`. That
  the second reading holds the same rule is B1's measurement (3 misses over this note)
  and B5's (removing it drops them to 0), not this one's.

B5 - the reading inside `measure_all` removed:
--- /home/incubator/orchestra/notes_gen.py
-+ measure_placement(keys, placement_fields(body, front)))
+)  # MUTATION: placement removed from the assembled-body measurement
  placement misses over the same red note: 0 (was 3)
  This mutation leaves the early reading standing, so it does not show a capture running
  to completion over a red note - B3 is where that state is measured, from the history
  rather than from an edit. What it does show is that the second reading is the one that
  carries the rule: with it removed the assembled body measures clean, and the only
  thing left between this note and `stage()` is an accelerator B4 has just killed.

==================================================================================================
CLOSING CONTROL - the tree this run measured is the tree it leaves behind
==================================================================================================

The fixtures are removed, the manifest is restored from the bytes read before anything
was written, and the site is rebuilt by the gate run itself:

$ ./scripts/push.sh --gates-only
exit 0
All checks passed!
593 passed, 1 skipped in 20.00s
TREE GREEN (gates only, nothing pushed)

git status --porcelain, before and after:
  before: ['(clean)']
  after : ['(clean)']
  identical: True
  manifest sha256 unchanged: True

A red run that changed the tree it was measuring is not evidence, so this is a
precondition of the artefact being written rather than a note at the end of it.
