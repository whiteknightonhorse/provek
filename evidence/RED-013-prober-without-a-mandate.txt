# RED-013 - the prober without its mandate check, and the classifier without each of its controls
#
# Produced deliberately on 2026-08-20 to establish that the two laws landing with T-2.12 CAN fail
# (invariant 5). Each mutation is applied to src/prober/prober.py from a pristine copy, the anchor
# is grepped back out to prove it applied, __pycache__ is purged, the suite runs, and the file is
# restored - and each mutation's output is captured to its OWN file before any of them is joined
# into this one.
#
# THAT LAST STEP IS HERE BECAUSE THE PREVIOUS VERSION OF THIS FILE WAS WRONG. It was generated by
# one long shell block that mutated, ran and restored four times in sequence with all output
# redirected together, and the run recorded under RED 4 was in fact RED 3's - a different mutation's
# failures, published under a heading claiming to be verbatim output of this one. Fable caught it by
# applying the recorded mutation and comparing, which is the only way it could have been caught: the
# file looked exactly like an honest one. An evidence artefact exists to be checked against, so a
# fabricated line in it is worse than a missing file, and the generator is now structured so the
# outputs cannot be transposed.
#
# The four failure sets below are distinct, and that is itself the check: a mutation whose output
# matched another's would be visible here rather than hidden by a heading.
#
# Everything below each `$` line is verbatim output of the command shown.
#
# ============================================================================================
# RED 1. LAW-PROBE-NEEDS-MANDATE. The guard is left standing and stops nothing:
#
# -   if mandate is None or not ok:
# +   if False:
#
# The shape matters more than deleting the call: `may_probe` is still there for a reader of the
# diff, and eight assertions die anyway, because they are about the requests that left the process
# and not about the shape of the code.
#
# $ grep -n 'MUTATION' src/prober/prober.py
411:    if False:  # MUTATION: the guard no longer stops anything
#
# $ python3 -m pytest tests/test_prober.py -q
.FFFFF.FF..F....                                                         [100%]
=================================== FAILURES ===================================
______________ test_a_denied_mandate_makes_no_request[no_mandate] ______________

reason = <Denial.NO_MANDATE: 'no_mandate'>

    @pytest.mark.parametrize("reason", list(DENIALS))
    def test_a_denied_mandate_makes_no_request(reason: Denial) -> None:
        mandate, spent = DENIALS[reason]
        r = Recorder()
>       result = probe(CLAIM, mandate, NOW, r, calls_last_hour=spent)

tests/test_prober.py:101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

claim = ControlClaim(subject_id='git:a/b', origin='https://example.test', control_path='/', absent_path='/no-such-path', prote...e', method='GET', expected_refusal=frozenset({401}), claimed_by="README.md, 'the API refuses unauthenticated callers'")
mandate = None
now = datetime.datetime(2026, 8, 20, 0, 0, tzinfo=datetime.timezone.utc)
fetch = <tests.test_prober.Recorder object at 0x7f7ea115fa60>
calls_last_hour = 0

    def probe(claim: ControlClaim, mandate: Mandate | None, now: datetime,
              fetch: Callable[[str, str], Response], calls_last_hour: int = 0) -> ProbeResult:
        """Attempt one probe. Returns what was established, or the named reason nothing was.
    
        `fetch(method, url)` is the transport, and it is passed in rather than imported so that the
        only path from this function to the network runs through an argument the caller supplies. Every
        refusal below returns before `fetch` is named.
        """
        # THE MANDATE IS ASKED ABOUT THE LAST CALL THE PROBE WOULD MAKE, NOT THE FIRST.
        #
        # `may_probe` answers about one call: it denies when `calls_last_hour` has already reached the
        # ceiling. A probe that spends three calls and asks about one buys the right to make a single
        # request and then makes three, which overruns a ceiling that exists as the subject's protection -
        # the one number in the mandate that bounds what we cost them. Asking about the last call means
        # a probe that cannot afford its own control does not run at all, rather than running half of
        # itself and reporting the half as a reading.
        ok, why = may_probe(mandate, ACTION, now, calls_last_hour + CALLS_PER_PROBE - 1)
        if False:  # MUTATION: the guard no longer stops anything
            # `mandate is None` is tested first for the type checker's benefit and not the reader's:
            # `may_probe(None, ...)` already returned NO_MANDATE, so the two conditions cannot disagree.
            return ProbeResult(state=ProbeState.REFUSED_BY_MANDATE, claim=claim,
                               mandate_ref=mandate.ref if mandate is not None else None,
                               denial=why, calls_made=0)
    
        # BOTH CONTROLS ARE TAKEN WITH THE SAME METHOD THE CLAIM USES, and the negative one is asked
        # that way for a reason: an origin can route GET and POST to different handlers, so a catch-all
        # established with the wrong verb is a catch-all for a question nobody asked.
        control = fetch("GET", claim.control_url())
        absent = fetch(claim.method, claim.absent_url())
        subject = fetch(claim.method, claim.protected_url())
        return ProbeResult(state=classify(claim, control, absent, subject), claim=claim,
>                          mandate_ref=mandate.ref, control=control, absent=absent, subject=subject,
                           calls_made=CALLS_PER_PROBE)
E       AttributeError: 'NoneType' object has no attribute 'ref'

src/prober/prober.py:425: AttributeError
_______________ test_a_denied_mandate_makes_no_request[revoked] ________________

reason = <Denial.REVOKED: 'revoked'>

    @pytest.mark.parametrize("reason", list(DENIALS))
    def test_a_denied_mandate_makes_no_request(reason: Denial) -> None:
        mandate, spent = DENIALS[reason]
        r = Recorder()
        result = probe(CLAIM, mandate, NOW, r, calls_last_hour=spent)
>       assert r.calls == [], (
            f"the prober contacted {r.calls} while the mandate said {reason.value}. Probing a live "
            "system without permission is an incident and not a verification: the request is the harm, "
            "and a check that runs after it has already gone out prevents nothing."
        )
E       AssertionError: the prober contacted [('GET', 'https://example.test/'), ('GET', 'https://example.test/no-such-path'), ('GET', 'https://example.test/api/private')] while the mandate said revoked. Probing a live system without permission is an incident and not a verification: the request is the harm, and a check that runs after it has already gone out prevents nothing.
E       assert [('GET', 'htt...api/private')] == []
E         
E         Left contains 3 more items, first extra item: ('GET', 'https://example.test/')
E         Use -v to get more diff

tests/test_prober.py:102: AssertionError
_______________ test_a_denied_mandate_makes_no_request[expired] ________________

reason = <Denial.EXPIRED: 'expired'>

    @pytest.mark.parametrize("reason", list(DENIALS))
    def test_a_denied_mandate_makes_no_request(reason: Denial) -> None:
        mandate, spent = DENIALS[reason]
        r = Recorder()
        result = probe(CLAIM, mandate, NOW, r, calls_last_hour=spent)
>       assert r.calls == [], (
            f"the prober contacted {r.calls} while the mandate said {reason.value}. Probing a live "
            "system without permission is an incident and not a verification: the request is the harm, "
            "and a check that runs after it has already gone out prevents nothing."
        )
E       AssertionError: the prober contacted [('GET', 'https://example.test/'), ('GET', 'https://example.test/no-such-path'), ('GET', 'https://example.test/api/private')] while the mandate said expired. Probing a live system without permission is an incident and not a verification: the request is the harm, and a check that runs after it has already gone out prevents nothing.
E       assert [('GET', 'htt...api/private')] == []
E         
E         Left contains 3 more items, first extra item: ('GET', 'https://example.test/')
E         Use -v to get more diff

tests/test_prober.py:102: AssertionError
_________ test_a_denied_mandate_makes_no_request[action_not_permitted] _________

reason = <Denial.ACTION_NOT_PERMITTED: 'action_not_permitted'>

    @pytest.mark.parametrize("reason", list(DENIALS))
    def test_a_denied_mandate_makes_no_request(reason: Denial) -> None:
        mandate, spent = DENIALS[reason]
        r = Recorder()
        result = probe(CLAIM, mandate, NOW, r, calls_last_hour=spent)
>       assert r.calls == [], (
            f"the prober contacted {r.calls} while the mandate said {reason.value}. Probing a live "
            "system without permission is an incident and not a verification: the request is the harm, "
            "and a check that runs after it has already gone out prevents nothing."
        )
E       AssertionError: the prober contacted [('GET', 'https://example.test/'), ('GET', 'https://example.test/no-such-path'), ('GET', 'https://example.test/api/private')] while the mandate said action_not_permitted. Probing a live system without permission is an incident and not a verification: the request is the harm, and a check that runs after it has already gone out prevents nothing.
E       assert [('GET', 'htt...api/private')] == []
E         
E         Left contains 3 more items, first extra item: ('GET', 'https://example.test/')
E         Use -v to get more diff

tests/test_prober.py:102: AssertionError
____________ test_a_denied_mandate_makes_no_request[rate_exceeded] _____________

reason = <Denial.RATE_EXCEEDED: 'rate_exceeded'>

    @pytest.mark.parametrize("reason", list(DENIALS))
    def test_a_denied_mandate_makes_no_request(reason: Denial) -> None:
        mandate, spent = DENIALS[reason]
        r = Recorder()
        result = probe(CLAIM, mandate, NOW, r, calls_last_hour=spent)
>       assert r.calls == [], (
            f"the prober contacted {r.calls} while the mandate said {reason.value}. Probing a live "
            "system without permission is an incident and not a verification: the request is the harm, "
            "and a check that runs after it has already gone out prevents nothing."
        )
E       AssertionError: the prober contacted [('GET', 'https://example.test/'), ('GET', 'https://example.test/no-such-path'), ('GET', 'https://example.test/api/private')] while the mandate said rate_exceeded. Probing a live system without permission is an incident and not a verification: the request is the harm, and a check that runs after it has already gone out prevents nothing.
E       assert [('GET', 'htt...api/private')] == []
E         
E         Left contains 3 more items, first extra item: ('GET', 'https://example.test/')
E         Use -v to get more diff

tests/test_prober.py:102: AssertionError
______ test_a_refusal_is_NOT_MEASURED_and_never_a_failure_of_the_subject _______

    def test_a_refusal_is_NOT_MEASURED_and_never_a_failure_of_the_subject() -> None:
        """Invariant 1 and ABI-33-4: "we could not measure" is not "the subject failed"."""
>       result = probe(CLAIM, None, NOW, Recorder())

tests/test_prober.py:122: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

claim = ControlClaim(subject_id='git:a/b', origin='https://example.test', control_path='/', absent_path='/no-such-path', prote...e', method='GET', expected_refusal=frozenset({401}), claimed_by="README.md, 'the API refuses unauthenticated callers'")
mandate = None
now = datetime.datetime(2026, 8, 20, 0, 0, tzinfo=datetime.timezone.utc)
fetch = <tests.test_prober.Recorder object at 0x7f7ea0f98e50>
calls_last_hour = 0

    def probe(claim: ControlClaim, mandate: Mandate | None, now: datetime,
              fetch: Callable[[str, str], Response], calls_last_hour: int = 0) -> ProbeResult:
        """Attempt one probe. Returns what was established, or the named reason nothing was.
    
        `fetch(method, url)` is the transport, and it is passed in rather than imported so that the
        only path from this function to the network runs through an argument the caller supplies. Every
        refusal below returns before `fetch` is named.
        """
        # THE MANDATE IS ASKED ABOUT THE LAST CALL THE PROBE WOULD MAKE, NOT THE FIRST.
        #
        # `may_probe` answers about one call: it denies when `calls_last_hour` has already reached the
        # ceiling. A probe that spends three calls and asks about one buys the right to make a single
        # request and then makes three, which overruns a ceiling that exists as the subject's protection -
        # the one number in the mandate that bounds what we cost them. Asking about the last call means
        # a probe that cannot afford its own control does not run at all, rather than running half of
        # itself and reporting the half as a reading.
        ok, why = may_probe(mandate, ACTION, now, calls_last_hour + CALLS_PER_PROBE - 1)
        if False:  # MUTATION: the guard no longer stops anything
            # `mandate is None` is tested first for the type checker's benefit and not the reader's:
            # `may_probe(None, ...)` already returned NO_MANDATE, so the two conditions cannot disagree.
            return ProbeResult(state=ProbeState.REFUSED_BY_MANDATE, claim=claim,
                               mandate_ref=mandate.ref if mandate is not None else None,
                               denial=why, calls_made=0)
    
        # BOTH CONTROLS ARE TAKEN WITH THE SAME METHOD THE CLAIM USES, and the negative one is asked
        # that way for a reason: an origin can route GET and POST to different handlers, so a catch-all
        # established with the wrong verb is a catch-all for a question nobody asked.
        control = fetch("GET", claim.control_url())
        absent = fetch(claim.method, claim.absent_url())
        subject = fetch(claim.method, claim.protected_url())
        return ProbeResult(state=classify(claim, control, absent, subject), claim=claim,
>                          mandate_ref=mandate.ref, control=control, absent=absent, subject=subject,
                           calls_made=CALLS_PER_PROBE)
E       AttributeError: 'NoneType' object has no attribute 'ref'

src/prober/prober.py:425: AttributeError
___________ test_a_refusal_for_want_of_a_document_names_no_document ____________

    def test_a_refusal_for_want_of_a_document_names_no_document() -> None:
        """`mandate_ref` is None only where there was no mandate; every other state carries one."""
>       assert probe(CLAIM, None, NOW, Recorder()).mandate_ref is None

tests/test_prober.py:129: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

claim = ControlClaim(subject_id='git:a/b', origin='https://example.test', control_path='/', absent_path='/no-such-path', prote...e', method='GET', expected_refusal=frozenset({401}), claimed_by="README.md, 'the API refuses unauthenticated callers'")
mandate = None
now = datetime.datetime(2026, 8, 20, 0, 0, tzinfo=datetime.timezone.utc)
fetch = <tests.test_prober.Recorder object at 0x7f7ea1316050>
calls_last_hour = 0

    def probe(claim: ControlClaim, mandate: Mandate | None, now: datetime,
              fetch: Callable[[str, str], Response], calls_last_hour: int = 0) -> ProbeResult:
        """Attempt one probe. Returns what was established, or the named reason nothing was.
    
        `fetch(method, url)` is the transport, and it is passed in rather than imported so that the
        only path from this function to the network runs through an argument the caller supplies. Every
        refusal below returns before `fetch` is named.
        """
        # THE MANDATE IS ASKED ABOUT THE LAST CALL THE PROBE WOULD MAKE, NOT THE FIRST.
        #
        # `may_probe` answers about one call: it denies when `calls_last_hour` has already reached the
        # ceiling. A probe that spends three calls and asks about one buys the right to make a single
        # request and then makes three, which overruns a ceiling that exists as the subject's protection -
        # the one number in the mandate that bounds what we cost them. Asking about the last call means
        # a probe that cannot afford its own control does not run at all, rather than running half of
        # itself and reporting the half as a reading.
        ok, why = may_probe(mandate, ACTION, now, calls_last_hour + CALLS_PER_PROBE - 1)
        if False:  # MUTATION: the guard no longer stops anything
            # `mandate is None` is tested first for the type checker's benefit and not the reader's:
            # `may_probe(None, ...)` already returned NO_MANDATE, so the two conditions cannot disagree.
            return ProbeResult(state=ProbeState.REFUSED_BY_MANDATE, claim=claim,
                               mandate_ref=mandate.ref if mandate is not None else None,
                               denial=why, calls_made=0)
    
        # BOTH CONTROLS ARE TAKEN WITH THE SAME METHOD THE CLAIM USES, and the negative one is asked
        # that way for a reason: an origin can route GET and POST to different handlers, so a catch-all
        # established with the wrong verb is a catch-all for a question nobody asked.
        control = fetch("GET", claim.control_url())
        absent = fetch(claim.method, claim.absent_url())
        subject = fetch(claim.method, claim.protected_url())
        return ProbeResult(state=classify(claim, control, absent, subject), claim=claim,
>                          mandate_ref=mandate.ref, control=control, absent=absent, subject=subject,
                           calls_made=CALLS_PER_PROBE)
E       AttributeError: 'NoneType' object has no attribute 'ref'

src/prober/prober.py:425: AttributeError
_________ test_a_probe_that_cannot_afford_its_own_control_does_not_run _________

    def test_a_probe_that_cannot_afford_its_own_control_does_not_run() -> None:
        """The ceiling in a mandate bounds what we cost the subject, and it is spent in whole probes.
    
        Two calls left of a ceiling of three is not enough for a probe that needs three, and the honest
        answer is to make none. `may_probe` answers about a single call, so a prober that asked about
        the FIRST of its three would buy the right to one request and then make three - overrunning the
        only number in the document that limits what the subject pays for being verified.
        """
        r = Recorder()
        result = probe(CLAIM, _mandate(max_calls_per_hour=3), NOW, r, calls_last_hour=1)
>       assert r.calls == []
E       AssertionError: assert [('GET', 'htt...api/private')] == []
E         
E         Left contains 3 more items, first extra item: ('GET', 'https://example.test/')
E         Use -v to get more diff

tests/test_prober.py:178: AssertionError
=========================== short test summary info ============================
FAILED tests/test_prober.py::test_a_denied_mandate_makes_no_request[no_mandate]
FAILED tests/test_prober.py::test_a_denied_mandate_makes_no_request[revoked]
FAILED tests/test_prober.py::test_a_denied_mandate_makes_no_request[expired]
FAILED tests/test_prober.py::test_a_denied_mandate_makes_no_request[action_not_permitted]
FAILED tests/test_prober.py::test_a_denied_mandate_makes_no_request[rate_exceeded]
FAILED tests/test_prober.py::test_a_refusal_is_NOT_MEASURED_and_never_a_failure_of_the_subject
FAILED tests/test_prober.py::test_a_refusal_for_want_of_a_document_names_no_document
FAILED tests/test_prober.py::test_a_probe_that_cannot_afford_its_own_control_does_not_run
8 failed, 8 passed in 0.09s
#
# ============================================================================================
# RED 2. LAW-PROBE-CONTROL-BEFORE-ABSENCE, the POSITIVE control. The request is still made and
# still costs the subject a call; it is simply never consulted:
#
# -   if not control.succeeded:
# +   if False:
#
# The first failure is the one L-11 was written about: the origin answered 403 to our client on
# every path, the probed path's reply was read as a measurement, and the subject was credited with
# a control that was never exercised.
#
# $ grep -n 'MUTATION' src/prober/prober.py
316:    if False:  # MUTATION: positive control taken and never consulted
#
# $ python3 -m pytest tests/test_probe_control.py -q
.FF......F................................                               [100%]
=================================== FAILURES ===================================
_ test_a_refused_control_voids_a_reading_that_would_have_flattered_the_subject _

    def test_a_refused_control_voids_a_reading_that_would_have_flattered_the_subject() -> None:
        """The exact L-11 failure: the edge refuses our client, and every path answers a refusal.
    
        The protected path returned 405 - precisely what the claim predicts - and it must NOT be read
        as the control being enforced, because the origin was not answering this client at all.
        """
        state = classify(CLAIM, Response(403, "cloudflare refused the client"), GONE,
                         Response(405, "method not allowed"))
>       assert state is ProbeState.ORIGIN_UNREADABLE, (
            "a 403 on the control page means the origin is refusing US. Reading the protected path's "
            "reply as a measurement of the subject's control publishes our own blindness as their "
            "compliance - and in the direction that flatters them, which is the direction nobody "
            "checks."
        )
E       AssertionError: a 403 on the control page means the origin is refusing US. Reading the protected path's reply as a measurement of the subject's control publishes our own blindness as their compliance - and in the direction that flatters them, which is the direction nobody checks.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.ORIGIN_UNREADABLE: 'origin_unreadable'>
E        +  where <ProbeState.ORIGIN_UNREADABLE: 'origin_unreadable'> = ProbeState.ORIGIN_UNREADABLE

tests/test_probe_control.py:64: AssertionError
_ test_a_refused_control_also_voids_a_reading_that_would_have_accused_the_subject _

    def test_a_refused_control_also_voids_a_reading_that_would_have_accused_the_subject() -> None:
        """Symmetry, and it is the half that would cause real damage.
    
        A verifier that says "your control is not enforced" on the strength of a probe that could not
        reach the origin has published an accusation computed from its own failure.
        """
        state = classify(CLAIM, Response(None, "connection refused"), GONE,
                         Response(200, "served"))
>       assert state is ProbeState.ORIGIN_UNREADABLE
E       AssertionError: assert <ProbeState.NOT_ENFORCED: 'not_enforced'> is <ProbeState.ORIGIN_UNREADABLE: 'origin_unreadable'>
E        +  where <ProbeState.ORIGIN_UNREADABLE: 'origin_unreadable'> = ProbeState.ORIGIN_UNREADABLE

tests/test_probe_control.py:80: AssertionError
______ test_a_declining_path_on_a_readable_origin_is_not_a_refused_origin ______

    def test_a_declining_path_on_a_readable_origin_is_not_a_refused_origin() -> None:
        """The two "we could not tell" states are two facts, and Fable found them sharing one name.
    
        `ORIGIN_UNREADABLE` means this origin will not talk to us - the remedy is to ask again as a
        different client. `SUBJECT_DECLINED` means the origin talks to us everywhere except this path -
        the remedy is to ask the subject what sits in front of it. Collapsing them left the caller to
        guess which, in the classifier written to stop exactly that collapse.
        """
        declined = classify(CLAIM, OK, GONE, Response(403, "forbidden"))
        refused_origin = classify(CLAIM, Response(403, "edge refused the client"), GONE,
                                  Response(403, "x"))
        assert declined is ProbeState.SUBJECT_DECLINED
>       assert refused_origin is ProbeState.ORIGIN_UNREADABLE
E       AssertionError: assert <ProbeState.SUBJECT_DECLINED: 'subject_declined'> is <ProbeState.ORIGIN_UNREADABLE: 'origin_unreadable'>
E        +  where <ProbeState.ORIGIN_UNREADABLE: 'origin_unreadable'> = ProbeState.ORIGIN_UNREADABLE

tests/test_probe_control.py:102: AssertionError
=========================== short test summary info ============================
FAILED tests/test_probe_control.py::test_a_refused_control_voids_a_reading_that_would_have_flattered_the_subject
FAILED tests/test_probe_control.py::test_a_refused_control_also_voids_a_reading_that_would_have_accused_the_subject
FAILED tests/test_probe_control.py::test_a_declining_path_on_a_readable_origin_is_not_a_refused_origin
3 failed, 39 passed in 0.09s
#
# ============================================================================================
# RED 3. The NEGATIVE control's comparison, never made:
#
# -   reading_matches_absence = not catch_all_known or absent.status == subject.status
# +   reading_matches_absence = False
#
# This approximates the state shipped between refutation rounds 1 and 2 at the level of the
# classifier's decisions - it is NOT that code restored. The request to the absent path is still
# made and still costs the subject a call, whereas the version actually shipped made two calls and
# `classify` had no `absent` parameter at all. The equivalence claimed is over verdicts, not over
# the diff.
#
# $ grep -n 'MUTATION' src/prober/prober.py
346:    reading_matches_absence = False  # MUTATION: the negative control is never compared
#
# $ python3 -m pytest tests/test_probe_control.py -q
.............FFFFFFFFFFF..................                               [100%]
=================================== FAILURES ===================================
_ test_divergence_is_never_published_over_an_unestablished_catch_all[absent0-subject0-an origin that redirects every unknown path - the commonest catch-all after 404] _

absent = Response(status=301, reason='redirected')
subject = Response(status=301, reason='redirected')
why = 'an origin that redirects every unknown path - the commonest catch-all after 404'

    @pytest.mark.parametrize(("absent", "subject", "why"), [
        (Response(301, "redirected"), Response(301, "redirected"),
         "an origin that redirects every unknown path - the commonest catch-all after 404"),
        (Response(404, "not found"), Response(404, "not found"),
         "the probed path answers exactly as a path that never existed"),
        (Response(None, "connection reset"), Response(410, "gone"),
         "the negative control never answered, so no catch-all was established"),
        (Response(503, "the edge declined"), Response(410, "gone"),
         "the negative control declined to say, which establishes no catch-all either"),
    ])
    def test_divergence_is_never_published_over_an_unestablished_catch_all(
            absent: Response, subject: Response, why: str) -> None:
        """THE THIRD DOOR, AND IT WAS OPEN FOR A ROUND. Fable found it after the repair that closed the
        other two.
    
        Both vetoes were written inline at ENFORCED and at NOT_ENFORCED, and the fall-through to
        DIVERGED had neither - so a host that had deployed NOTHING, answering every path with the same
        redirect, was accused of divergence on a measured FAIL. That is the mirror image of the
        sentence `INDISTINGUISHABLE_FROM_ABSENT` exists for, and it survived two rounds of repairing
        the same defect in its two neighbours: the rule had two copies and a third place that needed it
        (L-2, inside one function).
        """
        state = classify(CLAIM, OK, absent, subject)
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, f"{why}: got {state.value}"
E       AssertionError: an origin that redirects every unknown path - the commonest catch-all after 404: got diverged
E       assert <ProbeState.DIVERGED: 'diverged'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:163: AssertionError
_ test_divergence_is_never_published_over_an_unestablished_catch_all[absent1-subject1-the probed path answers exactly as a path that never existed] _

absent = Response(status=404, reason='not found')
subject = Response(status=404, reason='not found')
why = 'the probed path answers exactly as a path that never existed'

    @pytest.mark.parametrize(("absent", "subject", "why"), [
        (Response(301, "redirected"), Response(301, "redirected"),
         "an origin that redirects every unknown path - the commonest catch-all after 404"),
        (Response(404, "not found"), Response(404, "not found"),
         "the probed path answers exactly as a path that never existed"),
        (Response(None, "connection reset"), Response(410, "gone"),
         "the negative control never answered, so no catch-all was established"),
        (Response(503, "the edge declined"), Response(410, "gone"),
         "the negative control declined to say, which establishes no catch-all either"),
    ])
    def test_divergence_is_never_published_over_an_unestablished_catch_all(
            absent: Response, subject: Response, why: str) -> None:
        """THE THIRD DOOR, AND IT WAS OPEN FOR A ROUND. Fable found it after the repair that closed the
        other two.
    
        Both vetoes were written inline at ENFORCED and at NOT_ENFORCED, and the fall-through to
        DIVERGED had neither - so a host that had deployed NOTHING, answering every path with the same
        redirect, was accused of divergence on a measured FAIL. That is the mirror image of the
        sentence `INDISTINGUISHABLE_FROM_ABSENT` exists for, and it survived two rounds of repairing
        the same defect in its two neighbours: the rule had two copies and a third place that needed it
        (L-2, inside one function).
        """
        state = classify(CLAIM, OK, absent, subject)
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, f"{why}: got {state.value}"
E       AssertionError: the probed path answers exactly as a path that never existed: got diverged
E       assert <ProbeState.DIVERGED: 'diverged'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:163: AssertionError
_ test_divergence_is_never_published_over_an_unestablished_catch_all[absent2-subject2-the negative control never answered, so no catch-all was established] _

absent = Response(status=None, reason='connection reset')
subject = Response(status=410, reason='gone')
why = 'the negative control never answered, so no catch-all was established'

    @pytest.mark.parametrize(("absent", "subject", "why"), [
        (Response(301, "redirected"), Response(301, "redirected"),
         "an origin that redirects every unknown path - the commonest catch-all after 404"),
        (Response(404, "not found"), Response(404, "not found"),
         "the probed path answers exactly as a path that never existed"),
        (Response(None, "connection reset"), Response(410, "gone"),
         "the negative control never answered, so no catch-all was established"),
        (Response(503, "the edge declined"), Response(410, "gone"),
         "the negative control declined to say, which establishes no catch-all either"),
    ])
    def test_divergence_is_never_published_over_an_unestablished_catch_all(
            absent: Response, subject: Response, why: str) -> None:
        """THE THIRD DOOR, AND IT WAS OPEN FOR A ROUND. Fable found it after the repair that closed the
        other two.
    
        Both vetoes were written inline at ENFORCED and at NOT_ENFORCED, and the fall-through to
        DIVERGED had neither - so a host that had deployed NOTHING, answering every path with the same
        redirect, was accused of divergence on a measured FAIL. That is the mirror image of the
        sentence `INDISTINGUISHABLE_FROM_ABSENT` exists for, and it survived two rounds of repairing
        the same defect in its two neighbours: the rule had two copies and a third place that needed it
        (L-2, inside one function).
        """
        state = classify(CLAIM, OK, absent, subject)
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, f"{why}: got {state.value}"
E       AssertionError: the negative control never answered, so no catch-all was established: got diverged
E       assert <ProbeState.DIVERGED: 'diverged'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:163: AssertionError
_ test_divergence_is_never_published_over_an_unestablished_catch_all[absent3-subject3-the negative control declined to say, which establishes no catch-all either] _

absent = Response(status=503, reason='the edge declined')
subject = Response(status=410, reason='gone')
why = 'the negative control declined to say, which establishes no catch-all either'

    @pytest.mark.parametrize(("absent", "subject", "why"), [
        (Response(301, "redirected"), Response(301, "redirected"),
         "an origin that redirects every unknown path - the commonest catch-all after 404"),
        (Response(404, "not found"), Response(404, "not found"),
         "the probed path answers exactly as a path that never existed"),
        (Response(None, "connection reset"), Response(410, "gone"),
         "the negative control never answered, so no catch-all was established"),
        (Response(503, "the edge declined"), Response(410, "gone"),
         "the negative control declined to say, which establishes no catch-all either"),
    ])
    def test_divergence_is_never_published_over_an_unestablished_catch_all(
            absent: Response, subject: Response, why: str) -> None:
        """THE THIRD DOOR, AND IT WAS OPEN FOR A ROUND. Fable found it after the repair that closed the
        other two.
    
        Both vetoes were written inline at ENFORCED and at NOT_ENFORCED, and the fall-through to
        DIVERGED had neither - so a host that had deployed NOTHING, answering every path with the same
        redirect, was accused of divergence on a measured FAIL. That is the mirror image of the
        sentence `INDISTINGUISHABLE_FROM_ABSENT` exists for, and it survived two rounds of repairing
        the same defect in its two neighbours: the rule had two copies and a third place that needed it
        (L-2, inside one function).
        """
        state = classify(CLAIM, OK, absent, subject)
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, f"{why}: got {state.value}"
E       AssertionError: the negative control declined to say, which establishes no catch-all either: got diverged
E       assert <ProbeState.DIVERGED: 'diverged'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:163: AssertionError
____ test_a_refusal_matching_the_catch_all_is_not_a_control_being_enforced _____

    def test_a_refusal_matching_the_catch_all_is_not_a_control_being_enforced() -> None:
        """THE HOLE FABLE FOUND IN THE FIRST SHIPPED CLAIM, and it is the case this project exists for.
    
        The subject claims a path refuses with 404. It answers 404 - and so does a path that was never
        there. The reading says the path is NOT SERVED, which may be because the subject withholds it
        or because it never existed, and telling those apart is the entire job. Publishing ENFORCED
        would credit a refusal never performed; a host that deployed nothing at all, or a claim with a
        misspelt path, would earn the same verdict.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
>       assert classify(claim, OK, GONE, Response(404, "not found")) is (
            ProbeState.INDISTINGUISHABLE_FROM_ABSENT)
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.ENFORCED: 'enforced'> = classify(ControlClaim(subject_id='git:a/b', origin='https://example.test', control_path='/', absent_path='/no-such-path', prote...ig', method='GET', expected_refusal=frozenset({404}), claimed_by="wrangler.toml, 'only the build output is published'"), Response(status=200, reason='control page served'), Response(status=404, reason='catch-all for a path that was never there'), Response(status=404, reason='not found'))
E        +    where Response(status=404, reason='not found') = Response(404, 'not found')
E        +  and   <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:181: AssertionError
____ test_an_unanswered_negative_control_never_upgrades_a_reading_to_a_pass ____

    def test_an_unanswered_negative_control_never_upgrades_a_reading_to_a_pass() -> None:
        """If the catch-all was not established, the comparison cannot be made - and the missing
        comparison lands in "we could not tell", not in the flattering state."""
>       assert classify(CLAIM, OK, Response(None, "connection reset"),
                        Response(405, "method not allowed")) is (
            ProbeState.INDISTINGUISHABLE_FROM_ABSENT)
E       AssertionError: assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.ENFORCED: 'enforced'> = classify(ControlClaim(subject_id='git:a/b', origin='https://example.test', control_path='/', absent_path='/no-such-path', prote...apply', method='GET', expected_refusal=frozenset({405}), claimed_by='functions/api/apply.js, onRequestGet returns 405'), Response(status=200, reason='control page served'), Response(status=None, reason='connection reset'), Response(status=405, reason='method not allowed'))
E        +    where Response(status=None, reason='connection reset') = Response(None, 'connection reset')
E        +    and   Response(status=405, reason='method not allowed') = Response(405, 'method not allowed')
E        +  and   <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:191: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[403] __

status = 403

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 403 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[429] __

status = 429

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 429 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[500] __

status = 500

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 500 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[503] __

status = 503

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 503 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__________ test_an_unestablished_catch_all_also_blocks_an_accusation ___________

    def test_an_unestablished_catch_all_also_blocks_an_accusation() -> None:
        """The veto is symmetric, and this half protects the subject rather than us.
    
        A 200 on the probed path means the path is served only if the origin does not answer 200 to
        everything. With the catch-all unestablished, a soft 404 has not been ruled out, and
        NOT_ENFORCED would be an accusation computed from a reading we failed to take.
        """
        state = classify(CLAIM, OK, Response(503, "the edge declined"), Response(200, "served a body"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT
E       AssertionError: assert <ProbeState.NOT_ENFORCED: 'not_enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:230: AssertionError
=========================== short test summary info ============================
FAILED tests/test_probe_control.py::test_divergence_is_never_published_over_an_unestablished_catch_all[absent0-subject0-an origin that redirects every unknown path - the commonest catch-all after 404]
FAILED tests/test_probe_control.py::test_divergence_is_never_published_over_an_unestablished_catch_all[absent1-subject1-the probed path answers exactly as a path that never existed]
FAILED tests/test_probe_control.py::test_divergence_is_never_published_over_an_unestablished_catch_all[absent2-subject2-the negative control never answered, so no catch-all was established]
FAILED tests/test_probe_control.py::test_divergence_is_never_published_over_an_unestablished_catch_all[absent3-subject3-the negative control declined to say, which establishes no catch-all either]
FAILED tests/test_probe_control.py::test_a_refusal_matching_the_catch_all_is_not_a_control_being_enforced
FAILED tests/test_probe_control.py::test_an_unanswered_negative_control_never_upgrades_a_reading_to_a_pass
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[403]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[429]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[500]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[503]
FAILED tests/test_probe_control.py::test_an_unestablished_catch_all_also_blocks_an_accusation
11 failed, 31 passed in 0.13s
#
# ============================================================================================
# RED 4. The negative control present but UNARMED - the state shipped between rounds 2 and 3, and
# the one no test caught at the time. `absent.answered` is not the same question as "is this
# origin's answer for absence known": 403, 429 and 5xx are the server declining to say, and a
# decline establishes no catch-all.
#
# -   catch_all_known = absent.answered and absent.status not in DECLINES_TO_SAY
# +   catch_all_known = absent.answered
#
# On an origin that refuses clients at its own discretion - which this subject demonstrably is -
# one unlucky call out of three would have promoted the reading to ENFORCED. Note that the failures
# here are NOT the same set as RED 3's: the equality comparison still runs, so a probed path
# answering exactly as the catch-all is still caught, and what breaks is only the declining case.
#
# $ grep -n 'MUTATION' src/prober/prober.py
336:    catch_all_known = absent.answered  # MUTATION: a decline counts as an established catch-all
#
# $ python3 -m pytest tests/test_probe_control.py -q
................F..FFFFF..................                               [100%]
=================================== FAILURES ===================================
_ test_divergence_is_never_published_over_an_unestablished_catch_all[absent3-subject3-the negative control declined to say, which establishes no catch-all either] _

absent = Response(status=503, reason='the edge declined')
subject = Response(status=410, reason='gone')
why = 'the negative control declined to say, which establishes no catch-all either'

    @pytest.mark.parametrize(("absent", "subject", "why"), [
        (Response(301, "redirected"), Response(301, "redirected"),
         "an origin that redirects every unknown path - the commonest catch-all after 404"),
        (Response(404, "not found"), Response(404, "not found"),
         "the probed path answers exactly as a path that never existed"),
        (Response(None, "connection reset"), Response(410, "gone"),
         "the negative control never answered, so no catch-all was established"),
        (Response(503, "the edge declined"), Response(410, "gone"),
         "the negative control declined to say, which establishes no catch-all either"),
    ])
    def test_divergence_is_never_published_over_an_unestablished_catch_all(
            absent: Response, subject: Response, why: str) -> None:
        """THE THIRD DOOR, AND IT WAS OPEN FOR A ROUND. Fable found it after the repair that closed the
        other two.
    
        Both vetoes were written inline at ENFORCED and at NOT_ENFORCED, and the fall-through to
        DIVERGED had neither - so a host that had deployed NOTHING, answering every path with the same
        redirect, was accused of divergence on a measured FAIL. That is the mirror image of the
        sentence `INDISTINGUISHABLE_FROM_ABSENT` exists for, and it survived two rounds of repairing
        the same defect in its two neighbours: the rule had two copies and a third place that needed it
        (L-2, inside one function).
        """
        state = classify(CLAIM, OK, absent, subject)
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, f"{why}: got {state.value}"
E       AssertionError: the negative control declined to say, which establishes no catch-all either: got diverged
E       assert <ProbeState.DIVERGED: 'diverged'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:163: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[403] __

status = 403

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 403 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[429] __

status = 429

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 429 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[500] __

status = 500

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 500 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__ test_a_negative_control_that_declines_to_say_establishes_no_catch_all[503] __

status = 503

    @pytest.mark.parametrize("status", [403, 429, 500, 503])
    def test_a_negative_control_that_declines_to_say_establishes_no_catch_all(status: int) -> None:
        """L-11 ON THE NEGATIVE CONTROL, and it was unarmed for one round.
    
        The condition was "did the negative control answer" - so a 403 counted as an answer, differed
        from the subject's 404, and the reading was promoted to ENFORCED. But 403, 429 and 5xx are the
        server declining to say: they establish no catch-all at all. On an origin that refuses clients
        at its own discretion - which this subject demonstrably is - one unlucky call out of the three
        would have credited it with a control it never exercised. That is the same defect the positive
        control exists to prevent, moved one request to the left, and it was introduced by the repair
        for the previous one.
        """
        claim = ControlClaim(
            subject_id="git:a/b", origin="https://example.test", control_path="/",
            absent_path="/no-such-path", protected_path="/.git/config", method="GET",
            expected_refusal=frozenset({404}),
            claimed_by="wrangler.toml, 'only the build output is published'",
        )
        state = classify(claim, OK, Response(status, "the edge declined"), Response(404, "not found"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT, (
            f"a negative control answering {status} did not establish what this origin returns for a "
            "path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting "
            "ENFORCED here credits a control that was never exercised."
        )
E       AssertionError: a negative control answering 503 did not establish what this origin returns for a path that does not exist, so the subject's 404 cannot be read as a refusal. Reporting ENFORCED here credits a control that was never exercised.
E       assert <ProbeState.ENFORCED: 'enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:215: AssertionError
__________ test_an_unestablished_catch_all_also_blocks_an_accusation ___________

    def test_an_unestablished_catch_all_also_blocks_an_accusation() -> None:
        """The veto is symmetric, and this half protects the subject rather than us.
    
        A 200 on the probed path means the path is served only if the origin does not answer 200 to
        everything. With the catch-all unestablished, a soft 404 has not been ruled out, and
        NOT_ENFORCED would be an accusation computed from a reading we failed to take.
        """
        state = classify(CLAIM, OK, Response(503, "the edge declined"), Response(200, "served a body"))
>       assert state is ProbeState.INDISTINGUISHABLE_FROM_ABSENT
E       AssertionError: assert <ProbeState.NOT_ENFORCED: 'not_enforced'> is <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'>
E        +  where <ProbeState.INDISTINGUISHABLE_FROM_ABSENT: 'indistinguishable_from_absent'> = ProbeState.INDISTINGUISHABLE_FROM_ABSENT

tests/test_probe_control.py:230: AssertionError
=========================== short test summary info ============================
FAILED tests/test_probe_control.py::test_divergence_is_never_published_over_an_unestablished_catch_all[absent3-subject3-the negative control declined to say, which establishes no catch-all either]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[403]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[429]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[500]
FAILED tests/test_probe_control.py::test_a_negative_control_that_declines_to_say_establishes_no_catch_all[503]
FAILED tests/test_probe_control.py::test_an_unestablished_catch_all_also_blocks_an_accusation
6 failed, 36 passed in 0.10s
